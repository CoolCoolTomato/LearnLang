# API Configuration
# Leave empty to use same-origin (recommended for production with reverse proxy)
VITE_API_BASE_URL=
VITE_API_PREFIX=/api/user
# Dev reverse proxy target (used by Vite server proxy)
VITE_DEV_API_PROXY_TARGET=http://localhost:8080

# Deployment Configuration
VITE_BASENAME=
